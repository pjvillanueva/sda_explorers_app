//Codes = T, M, S, P, Q, B

//L1 - lesson number
//T - text type code
//1 - placeInSequence
enum TextType { title, memoryVerse, subtitle, paragraph, question, bibleVerse }

addIndention(String text) {
  return '          $text';
}
